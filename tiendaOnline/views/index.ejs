<!DOCTYPE html>
<html lang="en">
<%- include('partials/head')%>

    <body>
        <nav>
            <%- include('partials/navBar')%>   
        </nav>
        <h2>INICIO DEL SITIO</h2>

        <section>
            <% for(let i=0; i<4; i+=1){ %>
                <article>
                    <h3>
                        <%= titulo[i].title %>
                    </h3>
                    <img src="<%= img[i].image %> " alt="">
                    <h4>Lanzamiento: <%= lanzamiento[i].upload %> </h4>

                    <%}%>
                </article>
        </section>
    </body>

</html>